# Propostas aprovadas

As propostas desta pasta foram autorizadas e aplicadas em 8 de setembro de 2026. Os arquivos SQL foram movidos para `supabase/migrations`, com as versões registradas pelo Supabase. O roteiro de testes foi movido para `tests/verify-marketplace.sql` e passou com `ROLLBACK`.

Consulte [DATABASE.md](../DATABASE.md) para o histórico e as permissões e [ROADMAP_STATUS.md](../ROADMAP_STATUS.md) para testes e pendências. Não execute novamente os scripts já aplicados ao projeto atual.
